# Osmosis Kakasi Plugin

## Usage

## Commands
```sh
# killing gradle daemon when it gets stuck
pkill -f '.*GradleDaemon.*'
# switching java versions
sudo update-alternatives --config java
```